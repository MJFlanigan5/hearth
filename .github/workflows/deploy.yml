name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy to /opt/kith
        run: |
          cd /opt/kith
          git pull
          docker compose build --no-cache
          docker compose up -d
