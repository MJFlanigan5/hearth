services:
  kith:
    build: .
    ports:
      - "7400:7400"
    volumes:
      - kith-data:/data
    restart: unless-stopped
    environment:
      - PORT=7400
      - DATA_DIR=/data
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - TZ=America/New_York

volumes:
  kith-data:
